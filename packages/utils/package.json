{
  "name": "@monorepo/utils",
  "version": "1.0.2",
  "main": "dist/index",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "license": "MIT",
  "devDependencies": {
    "@babel/preset-typescript": "^7.12.7",
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "@monorepo/types": "^1.0.2"
  },
  "scripts": {
    "build": "tsc -b .",
    "watch": "tsc -w",
    "clean": "rm -rf ./dist && rm -rf tsconfig.tsbuildinfo"
  }
}